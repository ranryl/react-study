{
    // tsconfig.json 是ts编译器的配置文件，ts编译器可以根据配置信息来对代码进行编译
    
    // 指定哪些ts文件需要被编译
    "include": [
        // ** 任意目录, * 任意文件
        "./src/**/*",
    ],
    // 排除
    "exclude": [
        // 默认值["node_modules", "bower_components", "jspm_packages"] 
    ],
    // 定义被继承的配置文件
    // "extends": "./configs/base",
    // 列举要编译的文件列表
    // "files":[],
    // 编译器配置
    "compilerOptions": {
        "target": "ES6",
        // 指定要使用的模块化的规范
        "module": "ES2015",
        // lib 指定代码运行时所包含的库(宿主环境)
        // 一般不需要设置
        // "lib": [],
        // 指定编译后的文件目录
        "outDir": "./dist",
        // 将代码合并为一个文件
        // 所有的全局作用域的代码会合并到同一个文件中
        // "outFile": "",
        // 是否对js文件进行编译，默认false
        "allowJs": true,
        // 是否符合js代码是否符合规范
        "checkJs": true,
        // 是否移出注释
        "removeComments": true,
        // 不生成编译后的文件
        "noEmit": false,
        // 当有错误时不生成编译文件
        "noEmitOnError": false,
        // 所有严格检查的总开关
        "strict": true,
        // 用来设置编译后的文件是否使用严格模式
        "alwaysStrict": false,
        // 不允许隐式的any
        "noImplicitAny": false,
        // 不允许不明确的this
        "noImplicitThis": false,
        // 严格检查空值
        "strictNullChecks": true,
    },
}